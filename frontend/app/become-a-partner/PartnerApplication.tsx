"use client";

import { FormEvent, useState } from "react";
import Link from "next/link";
import { motion } from "framer-motion";
import { ArrowLeft, ArrowRight, ArrowUpRight, Check, CheckCircle2, Loader2, ShieldCheck, Sparkles, Stars, TrendingUp } from "lucide-react";

const benefits = [{ icon: ShieldCheck, title: "Verified Premium Clients", copy: "Receive qualified event enquiries instead of spending on marketing." }, { icon: Stars, title: "Curated Network", copy: "Every partner is personally reviewed before joining." }, { icon: TrendingUp, title: "Long-Term Growth", copy: "Build lasting relationships with premium clients." }, { icon: Sparkles, title: "Focus on Your Craft", copy: "We handle customer acquisition while you focus on delivering exceptional experiences." }];
const steps = ["Studio", "Capabilities", "Experience", "Availability", "Portfolio"];
const eventTypes = ["Weddings", "Engagements", "Birthdays", "Corporate Events", "Housewarming", "Baby Shower", "Private Parties"];
const services = ["End-to-end planning", "Décor & styling", "Venue sourcing", "Photography", "Cinematography", "Drone coverage", "Live streaming", "Guest hospitality"];
type FormData = { partner_type: "event_partner" | "photography_studio" | ""; business_name: string; owner_name: string; email: string; phone: string; whatsapp: string; instagram: string; city: string; years_in_business: string; events_per_year: string; team_size: string; event_types: string[]; services: string[]; service_cities: string; travel_scope: string; min_event_budget: string; max_guest_count: string; minimum_notice_days: string; availability: string; premium_experience: string; portfolio_url: string; portfolio_highlights: string; client_references: string; business_description: string; differentiator: string; consent: boolean };
const empty: FormData = { partner_type: "", business_name: "", owner_name: "", email: "", phone: "", whatsapp: "", instagram: "", city: "", years_in_business: "", events_per_year: "", team_size: "", event_types: [], services: [], service_cities: "", travel_scope: "", min_event_budget: "", max_guest_count: "", minimum_notice_days: "", availability: "", premium_experience: "", portfolio_url: "", portfolio_highlights: "", client_references: "", business_description: "", differentiator: "", consent: false };
const input = "mt-2 w-full rounded-xl border border-line-strong bg-white/70 px-4 py-3.5 text-[15px] text-ink outline-none transition focus:border-burnt focus:ring-2 focus:ring-burnt/10";

function Field({ label, children, className = "" }: { label: string; children: React.ReactNode; className?: string }) { return <label className={`block ${className}`}><span className="text-[10px] tracking-[.2em] uppercase text-muted">{label}</span>{children}</label>; }
function Toggle({ value, selected, onChange }: { value: string; selected: string[]; onChange: () => void }) { return <button type="button" onClick={onChange} className={`rounded-full border px-4 py-2 text-sm transition ${selected.includes(value) ? "border-burnt bg-burnt text-cream" : "border-line-strong text-text hover:border-burnt"}`}>{value}</button>; }

export default function PartnerApplication() {
  const [form, setForm] = useState<FormData>(empty); const [page, setPage] = useState(0); const [submitting, setSubmitting] = useState(false); const [sent, setSent] = useState(false); const [error, setError] = useState<string | null>(null);
  const set = (key: keyof FormData, value: string | boolean | string[]) => setForm(old => ({ ...old, [key]: value }));
  const toggle = (key: "event_types" | "services", value: string) => set(key, form[key].includes(value) ? form[key].filter(item => item !== value) : [...form[key], value]);
  const pageValid = [Boolean(form.partner_type && form.business_name && form.owner_name && form.email && form.phone && form.whatsapp && form.instagram && form.city && form.years_in_business && form.events_per_year && form.team_size), Boolean(form.event_types.length && form.services.length && form.service_cities && form.travel_scope && form.min_event_budget && form.max_guest_count), Boolean(form.premium_experience && form.business_description && form.differentiator), Boolean(form.minimum_notice_days && form.availability), Boolean(form.portfolio_url && form.portfolio_highlights && form.client_references && form.consent)][page];
  async function submit(e: FormEvent) { e.preventDefault(); if (!pageValid || submitting) return; setSubmitting(true); setError(null); try { const payload = { ...form, service_cities: form.service_cities.split(",").map(city => city.trim()).filter(Boolean), max_guest_count: Number(form.max_guest_count), minimum_notice_days: Number(form.minimum_notice_days) }; const response = await fetch(`${process.env.NEXT_PUBLIC_BACKEND_URL ?? ""}/api/vendor-applications`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); if (!response.ok) { const body = await response.json().catch(() => null); throw new Error(body?.detail || "We couldn't submit your application."); } setSent(true); window.scrollTo({ top: 0, behavior: "smooth" }); } catch (err) { setError(err instanceof Error ? err.message : "Something went wrong."); } finally { setSubmitting(false); } }
  if (sent) return <section className="flex min-h-[78vh] items-center justify-center bg-cream px-6 pt-28"><motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="max-w-2xl text-center"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-full border border-gold/40 bg-cream-warm text-burnt"><CheckCircle2 size={34} /></div><p className="mt-8 eyebrow">SAMBARAM Partner Network</p><h1 className="mt-4 h-display text-5xl text-ink sm:text-7xl">Application Received</h1><p className="mt-7 text-lg leading-relaxed text-muted">Thank you for applying to become a SAMBARAM Partner. Our team personally reviews every application and will contact you if there is a fit.</p><Link href="/" className="btn-primary mt-10">Back to Home <ArrowUpRight size={16} className="ml-2" /></Link></motion.div></section>;
  return <><section className="relative overflow-hidden bg-cream pt-28 sm:pt-36"><div className="relative mx-auto grid max-w-7xl items-center gap-12 px-6 pb-20 sm:px-10 lg:grid-cols-2"><motion.div initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }}><div className="flex items-center gap-3"><span className="divider-line" /><span className="eyebrow">The Partner Network</span></div><h1 className="mt-5 h-display text-5xl leading-[.98] text-ink sm:text-7xl">Become a<br /><em className="text-burnt">SAMBARAM</em> Partner</h1><p className="mt-7 max-w-xl text-lg leading-relaxed text-muted">We&apos;re building a carefully curated network of trusted event companies and premium photography studios across India. If your work reflects craftsmanship, professionalism and attention to detail, we&apos;d love to hear from you.</p><button onClick={() => document.getElementById("application")?.scrollIntoView({ behavior: "smooth" })} className="btn-primary mt-9">Apply Now <ArrowRight size={16} className="ml-2" /></button></motion.div><div className="relative aspect-[4/5] overflow-hidden rounded-[2rem] bg-[url('https://images.unsplash.com/photo-1519167758481-83f550bb49b3?auto=format&fit=crop&w=1200&q=85')] bg-cover bg-center"><div className="absolute inset-x-5 bottom-5 rounded-2xl border border-white/20 bg-ink/70 px-5 py-4 text-[10px] tracking-[.2em] uppercase text-cream backdrop-blur">Crafted celebrations · India</div></div></div></section><section className="section bg-cream-warm/55"><div className="mx-auto max-w-7xl px-6 sm:px-10"><p className="eyebrow">Why partner with us</p><h2 className="mt-4 h-display text-4xl text-ink sm:text-6xl">A network built with intention.</h2><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{benefits.map(({ icon: Icon, title, copy }) => <motion.article key={title} whileHover={{ y: -5 }} className="lux-card p-7"><Icon className="text-burnt" size={22} /><h3 className="mt-8 font-serif text-2xl text-ink">{title}</h3><p className="mt-3 text-sm leading-relaxed text-muted">{copy}</p></motion.article>)}</div></div></section><section className="section bg-ink text-cream"><div className="mx-auto max-w-7xl px-6 sm:px-10"><p className="text-[11px] tracking-[.24em] uppercase text-gold">The selection process</p><h2 className="mt-4 h-display text-4xl sm:text-6xl">A personal introduction,<br /><em className="text-gold">not an automated queue.</em></h2><div className="mt-10 grid gap-4 md:grid-cols-5">{["Apply", "Portfolio Review", "Interview", "Verification", "Join SAMBARAM"].map((item, index) => <div key={item} className="border-t border-cream/20 pt-5"><span className="text-[10px] tracking-[.2em] text-gold">0{index + 1}</span><p className="mt-3 font-serif text-xl">{item}</p></div>)}</div></div></section>
  <section id="application" className="section scroll-mt-16 bg-cream-warm/45"><div className="mx-auto max-w-4xl px-6 sm:px-10"><div className="text-center"><p className="eyebrow">Begin your application</p><h2 className="mt-4 h-display text-4xl text-ink sm:text-6xl">Tell us about your studio.</h2><p className="mt-4 text-muted">Five considered steps help us understand the work you are best placed to deliver.</p></div><form onSubmit={submit} className="lux-card mt-10 overflow-hidden"><div className="flex overflow-x-auto border-b border-line px-5 sm:px-8">{steps.map((name, index) => <button key={name} type="button" onClick={() => index <= page && setPage(index)} className={`shrink-0 border-b-2 px-3 py-5 text-[10px] tracking-[.16em] uppercase ${index === page ? "border-burnt text-burnt" : "border-transparent text-muted"}`}>0{index + 1} · {name}</button>)}</div><div className="p-6 sm:p-10"><h3 className="font-serif text-3xl text-ink">{steps[page]}</h3><p className="mt-2 text-sm text-muted">Step {page + 1} of {steps.length}</p>{page === 0 && <div className="mt-8 grid gap-5 sm:grid-cols-2"><Field label="Partner Type"><select value={form.partner_type} onChange={e => set("partner_type", e.target.value)} className={input}><option value="">Select</option><option value="event_partner">Full-Service Event Partner</option><option value="photography_studio">Photography & Cinematography Studio</option></select></Field><Field label="Business Name"><input value={form.business_name} onChange={e => set("business_name", e.target.value)} className={input} /></Field><Field label="Owner Name"><input value={form.owner_name} onChange={e => set("owner_name", e.target.value)} className={input} /></Field><Field label="Email Address"><input type="email" value={form.email} onChange={e => set("email", e.target.value)} className={input} /></Field><Field label="Phone Number"><input value={form.phone} onChange={e => set("phone", e.target.value)} className={input} /></Field><Field label="WhatsApp Number"><input value={form.whatsapp} onChange={e => set("whatsapp", e.target.value)} className={input} /></Field><Field label="Instagram Profile"><input value={form.instagram} onChange={e => set("instagram", e.target.value)} className={input} /></Field><Field label="Primary City"><input value={form.city} onChange={e => set("city", e.target.value)} className={input} /></Field><Field label="Years in Business"><input type="number" value={form.years_in_business} onChange={e => set("years_in_business", e.target.value)} className={input} /></Field><Field label="Events Per Year"><input type="number" value={form.events_per_year} onChange={e => set("events_per_year", e.target.value)} className={input} /></Field><Field label="Team Size"><input type="number" value={form.team_size} onChange={e => set("team_size", e.target.value)} className={input} /></Field></div>}{page === 1 && <div className="mt-8 space-y-7"><div><p className="text-[10px] tracking-[.2em] uppercase text-muted">Event types you cover</p><div className="mt-3 flex flex-wrap gap-2">{eventTypes.map(value => <Toggle key={value} value={value} selected={form.event_types} onChange={() => toggle("event_types", value)} />)}</div></div><div><p className="text-[10px] tracking-[.2em] uppercase text-muted">Services you provide</p><div className="mt-3 flex flex-wrap gap-2">{services.map(value => <Toggle key={value} value={value} selected={form.services} onChange={() => toggle("services", value)} />)}</div></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Cities You Serve (comma separated)"><input value={form.service_cities} onChange={e => set("service_cities", e.target.value)} placeholder="Hyderabad, Warangal, Bengaluru" className={input} /></Field><Field label="Travel Scope"><select value={form.travel_scope} onChange={e => set("travel_scope", e.target.value)} className={input}><option value="">Select</option><option>Local only</option><option>Within my state</option><option>Pan India</option><option>International</option></select></Field><Field label="Minimum Event Budget"><input value={form.min_event_budget} onChange={e => set("min_event_budget", e.target.value)} placeholder="₹5L" className={input} /></Field><Field label="Maximum Guest Capacity"><input type="number" value={form.max_guest_count} onChange={e => set("max_guest_count", e.target.value)} className={input} /></Field></div></div>}{page === 2 && <div className="mt-8 space-y-6"><Field label="Premium Event Experience"><textarea rows={4} value={form.premium_experience} onChange={e => set("premium_experience", e.target.value)} placeholder="Share the kind of celebrations and clients you are known for." className={input} /></Field><Field label="Tell Us About Your Business"><textarea rows={4} value={form.business_description} onChange={e => set("business_description", e.target.value)} className={input} /></Field><Field label="What Makes Your Work Different?"><textarea rows={4} value={form.differentiator} onChange={e => set("differentiator", e.target.value)} className={input} /></Field></div>}{page === 3 && <div className="mt-8 grid gap-6 sm:grid-cols-2"><Field label="Minimum Notice Required (days)"><input type="number" value={form.minimum_notice_days} onChange={e => set("minimum_notice_days", e.target.value)} className={input} /></Field><Field label="Current Availability"><select value={form.availability} onChange={e => set("availability", e.target.value)} className={input}><option value="">Select</option><option>Available for new enquiries</option><option>Limited availability</option><option>Available from next season</option></select></Field></div>}{page === 4 && <div className="mt-8 space-y-6"><Field label="Portfolio Website / Google Drive Link"><input type="url" value={form.portfolio_url} onChange={e => set("portfolio_url", e.target.value)} className={input} /></Field><Field label="Three Portfolio Highlights"><textarea rows={4} value={form.portfolio_highlights} onChange={e => set("portfolio_highlights", e.target.value)} placeholder="A few events that represent your best work." className={input} /></Field><Field label="Client References or Reviews"><textarea rows={4} value={form.client_references} onChange={e => set("client_references", e.target.value)} className={input} /></Field><label className="flex items-start gap-3 text-sm text-muted"><input type="checkbox" checked={form.consent} onChange={e => set("consent", e.target.checked)} className="mt-1 accent-[#C04A24]" />I agree to be contacted by the SAMBARAM team regarding my application.</label></div>}{error && <p className="mt-5 text-sm text-burnt">{error}</p>}<div className="mt-10 flex justify-between gap-3">{page > 0 ? <button type="button" onClick={() => setPage(page - 1)} className="btn-ghost"><ArrowLeft size={15} className="mr-2" />Back</button> : <span />}{page < 4 ? <button type="button" disabled={!pageValid} onClick={() => setPage(page + 1)} className="btn-primary disabled:opacity-40">Continue <ArrowRight size={15} className="ml-2" /></button> : <button type="submit" disabled={!pageValid || submitting} className="btn-primary disabled:opacity-40">{submitting ? <><Loader2 size={16} className="mr-2 animate-spin" />Submitting</> : <>Submit Application <ArrowUpRight size={16} className="ml-2" /></>}</button>}</div></div></form></div></section></>;
}
